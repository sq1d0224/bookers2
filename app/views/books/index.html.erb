<h2>Books</h2>
<% @books.each do |book| %>
  <div>
    <p><%= image_tag book.user.get_profile_image(80,80) %></p>
    
    <%= link_to book_path(book.id) do %>
    <p><%= book.title %></p>
    <% end %>
    
    <p><%= book.body %></p>
  </div>
<% end %>
